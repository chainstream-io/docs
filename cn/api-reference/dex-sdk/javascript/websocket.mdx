---
title: 'WebSocket API'
description: 'WebSocket API 实时数据订阅服务'
---

本文档描述了 ChainStream DEX JavaScript SDK 提供的 WebSocket 订阅方法。每个方法都是对应 WebSocket API 的封装，详细的参数说明和返回值结构请点击对应的 API 链接查看。

## K线数据订阅

### 订阅K线数据
订阅代币价格K线数据。

<Tip>
  **WebSocket API:** [K线数据](/cn/api-reference/endpoint/websocket/api#k线数据)
</Tip>

```typescript
client.stream.subscribeTokenCandles()
```

## 代币数据订阅

### 订阅代币统计
订阅代币的市场统计数据。

<Tip>
  **WebSocket API:** [代币统计](/cn/api-reference/endpoint/websocket/api#代币统计)
</Tip>

```typescript
client.stream.subscribeTokenStats()
```

### 订阅热门代币统计
订阅热门代币的市场统计数据。

<Tip>
  **WebSocket API:** [热门代币统计](/cn/api-reference/endpoint/websocket/api#热门代币统计)
</Tip>

```typescript
client.stream.subscribeHotTokenStats()
```

### 订阅新币统计
订阅新上线代币的市场统计数据。

<Tip>
  **WebSocket API:** [新币统计](/cn/api-reference/endpoint/websocket/api#新币统计)
</Tip>

```typescript
client.stream.subscribeNewTokenStats()
```

### 订阅新代币元数据
订阅新上市代币的元数据信息。

<Tip>
  **WebSocket API:** [新代币元数据](/cn/api-reference/endpoint/websocket/api#新代币元数据)
</Tip>

```typescript
client.stream.subscribeNewTokensMetadata()
```

### 订阅新代币
订阅新创建代币的基本信息。

<Tip>
  **WebSocket API:** [新代币](/cn/api-reference/endpoint/websocket/api#新代币)
</Tip>

```typescript
client.stream.subscribeNewTokens()
```

### 订阅代币交易
订阅代币的交易事件。

<Tip>
  **WebSocket API:** [交易事件](/cn/api-reference/endpoint/websocket/api#交易事件)
</Tip>

```typescript
client.stream.subscribeTokenTrades()
```

### 订阅钱包交易
订阅钱包的交易事件。

<Tip>
  **WebSocket API:** [钱包交易事件](/cn/api-reference/endpoint/websocket/api#钱包交易事件)
</Tip>

```typescript
client.stream.subscribeWalletTrade()
```

### 订阅代币持有者
订阅代币的持有者统计信息。

<Tip>
  **WebSocket API:** [代币持有者统计](/cn/api-reference/endpoint/websocket/api#代币持有者统计)
</Tip>

```typescript
client.stream.subscribeTokenHolders()
```

### 订阅代币供应量
订阅代币的供应量和市值信息。

<Tip>
  **WebSocket API:** [代币供应量](/cn/api-reference/endpoint/websocket/api#代币供应量)
</Tip>

```typescript
client.stream.subscribeTokenSupply()
```

### 订阅 DEX 资金池余额
订阅 DEX 资金池的余额信息。

<Tip>
  **WebSocket API:** [DEX 资金池余额](/cn/api-reference/endpoint/websocket/api#dex-资金池余额)
</Tip>

```typescript
client.stream.subscribeDexPoolBalance()
```

### 订阅代币流动性
订阅代币的流动性统计信息。

<Tip>
  **WebSocket API:** [代币流动性](/cn/api-reference/endpoint/websocket/api#代币流动性)
</Tip>

```typescript
client.stream.subscribeTokenLiquidity()
```

## 钱包数据订阅

### 订阅钱包余额
订阅钱包的余额信息。

<Tip>
  **WebSocket API:** [钱包余额](/cn/api-reference/endpoint/websocket/api#钱包余额)
</Tip>

```typescript
client.stream.subscribeWalletBalance()
```

## 连接管理

### 建立连接
```typescript
client.stream.connect()
```

所有订阅方法都会返回一个 `Unsubscrible` 对象，可以用于取消订阅：

```typescript
const subscription = client.stream.subscribeTokenCandles(/* ... */);
// 取消订阅
subscription.unsubscribe();
```

详细的参数说明、数据格式和错误处理请参考 [WebSocket API 文档](/cn/api-reference/endpoint/websocket/api)。 