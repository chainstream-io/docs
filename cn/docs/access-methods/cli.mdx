---
title: "CLI"
description: "`chainstream` 命令行——以 shell 原语的形式访问每一个数据产品与 DeFi 原语。"
icon: "terminal"
---

ChainStream CLI 是你通过 SDK、REST、GraphQL 能访问的那些 [数据产品](/cn/docs/data-products/overview) 的命令行门面。它按 "和 `jq`、shell 管道、以及开 shell 的 AI Agent 一起用" 的思路设计。

## 安装

```bash
# macOS / Linux——Homebrew
brew install chainstream-io/tap/chainstream

# npm
npm install -g @chainstream-io/cli

# 直接下二进制
curl -fsSL https://chainstream.io/install.sh | sh
```

## 登录

```bash
chainstream auth login          # 浏览器 OAuth
# 或
export CHAINSTREAM_API_KEY=...  # 脚本里用环境变量
```

## 上手命令

```bash
chainstream tokens search --chain solana --query USDC --limit 5
chainstream trades tail --chain ethereum --token 0xA0b8...
chainstream rankings hot --chain solana --window 24h
chainstream defi quote --chain solana --from SOL --to USDC --amount 1
```

默认输出 JSON；人类可读加 `--format=table`。

## 什么时候用

- 快速探索（"现在发生什么？"）
- 脚本、cron、GitHub Actions
- 给 AI Agent 一套可执行的 ChainStream 原语
- 通过管道把 ChainStream 输出喂给其他 CLI 工具

## 下一步

<CardGroup cols={2}>
  <Card title="CLI 参考" icon="code" href="/cn/api-reference/cli-commands/overview">
    所有命令、参数与退出码。
  </Card>
  <Card title="Agent Skills" icon="brain" href="/cn/docs/ai-agents/agent-skills/introduction">
    AI Agent 该怎么用 CLI。
  </Card>
</CardGroup>
