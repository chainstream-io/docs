---
title: "GraphQL IDE"
description: "在浏览器中交互式探索、编写与测试 ChainStream GraphQL 查询"
icon: "code"
---

<Info>
ChainStream GraphQL IDE 是专为在 Solana、Ethereum 与 BSC 上编写、测试与分享 GraphQL 分析查询而打造的浏览器端开发环境。无需安装 — 打开 URL 即可开始查询。
</Info>

<Card title="打开 GraphQL IDE" icon="arrow-up-right-from-square" href="https://ide.chainstream.io">
  https://ide.chainstream.io
</Card>

---

## 功能亮点

<CardGroup cols={3}>
  <Card title="Monaco 编辑器" icon="pen-to-square">
    功能完备的代码编辑器：GraphQL 语法高亮、基于 schema 的自动补全，以及实时错误检测。
  </Card>
  <Card title="查询模板" icon="book-open">
    19 个内置模板，按链与 Cube 组织 — 秒级加载、自定义并执行。
  </Card>
  <Card title="保存查询" icon="bookmark">
    将常用查询连同标题与标签保存在本地，便于日后快速访问。
  </Card>
  <Card title="代码导出" icon="file-export">
    将查询导出为 8 种语言的即用代码片段：cURL、Python、JavaScript、Go、Rust、Ruby、PHP 与 WebSocket。
  </Card>
  <Card title="状态栏" icon="chart-simple">
    实时展示请求状态、延迟（ms）、响应体积，以及每次查询消耗的 Credit Units。
  </Card>
  <Card title="深色主题" icon="moon">
    与 ChainStream 品牌一致的深色界面，适合长时间编写查询。
  </Card>
</CardGroup>

---

## IDE 布局

IDE 分为五个区域：

```
┌──────────────────────────────────────────────────┐
│  Toolbar  [▶ Execute] [Prettify] [Copy] [Merge]  │
│           [Save Query] [Export Code]              │
├────────────┬─────────────────────────────────────┤
│            │                                     │
│  Sidebar   │         Query Editor                │
│            │                                     │
│ ┌────────┐ │─────────────────────────────────────│
│ │Queries │ │  Variables  │  Headers              │
│ │Saved   │ │─────────────────────────────────────│
│ └────────┘ │                                     │
│            │         Results Panel               │
│            │                                     │
├────────────┴─────────────────────────────────────┤
│  Status: Ready  │  120ms  │  2.4 KB  │  8 CU     │
└──────────────────────────────────────────────────┘
```

| 区域 | 说明 |
|:--|:--|
| **Toolbar** | Execute、Prettify、Copy、Merge、Save Query、Export Code 等按钮 |
| **Sidebar** | 两个标签页 — **Queries**（按链 → Cube 的内置模板）与 **Saved**（本地保存的查询） |
| **Query Editor** | 基于 Monaco 的 GraphQL 编辑器，支持自动补全与错误标记 |
| **Variables / Headers** | 底部面板：JSON 变量与 HTTP 请求头（默认显示 Headers，并预填 `X-API-KEY`） |
| **Results Panel** | GraphQL 端点返回的、带语法高亮的 JSON 响应 |
| **Status Bar** | 请求状态、延迟、响应体积，以及消耗的 Credit Units（CU） |

---

## 快速开始

<Steps>
  <Step title="打开 IDE">
    在浏览器中访问 [ide.chainstream.io](https://ide.chainstream.io)。
  </Step>
  <Step title="设置 API Key">
    点击编辑器底部的 **Headers** 面板，填入你的 API Key：
    ```json
    {
      "X-API-KEY": "your_api_key"
    }
    ```
    <Tip>
    还没有 Key？在 [ChainStream Dashboard](https://www.chainstream.io/dashboard) → **Applications** → **Create New App** 中创建。
    </Tip>
  </Step>
  <Step title="加载模板">
    打开侧边栏（**Ctrl/Cmd+B**），展开某条链（例如 **Solana**），再点击模板如 **DEXTrades → Latest Trades**。查询会载入编辑器并带有占位值。
  </Step>
  <Step title="替换占位符">
    将 `TOKEN_ADDRESS`、`WALLET_ADDRESS` 或 `POOL_ADDRESS` 等占位符替换为真实链上地址。
  </Step>
  <Step title="执行">
    按 **Ctrl/Cmd+Enter** 或点击 **Execute**（▶）按钮。结果出现在右侧面板，状态栏显示延迟与 CU 消耗。
  </Step>
</Steps>

---

## 延伸阅读

<CardGroup cols={2}>
  <Card title="查询编辑器" icon="pen-to-square" href="/cn/graphql/ide/query-editor">
    了解 Monaco 编辑器、面板、工具栏、快捷键与状态栏。
  </Card>
  <Card title="查询模板" icon="book-open" href="/cn/graphql/ide/query-templates">
    浏览 12 个 Cube 上全部 19 个内置模板。
  </Card>
  <Card title="保存的查询" icon="bookmark" href="/cn/graphql/ide/saved-queries">
    保存、整理并管理常用查询。
  </Card>
  <Card title="代码导出" icon="file-export" href="/cn/graphql/ide/code-export">
    生成 8 种编程语言的即用代码片段。
  </Card>
</CardGroup>
