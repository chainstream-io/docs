---
title: "CLI"
description: "`chainstream` コマンドラインインターフェース — シェルから全データプロダクトと DeFi プリミティブへ。"
icon: "terminal"
---

ChainStream CLI は、SDK / REST / GraphQL 経由で触れる [データプロダクト](/jp/docs/data-products/overview) への CLI フロント。`jq`・シェルパイプライン・シェルを駆動する AI Agent と組み合わせる設計です。

## インストール

```bash
brew install chainstream-io/tap/chainstream
npm install -g @chainstream-io/cli
curl -fsSL https://chainstream.io/install.sh | sh
```

## 認証

```bash
chainstream auth login
# または
export CHAINSTREAM_API_KEY=...
```

## 最初のコマンド

```bash
chainstream tokens search --chain solana --query USDC --limit 5
chainstream trades tail --chain ethereum --token 0xA0b8...
chainstream rankings hot --chain solana --window 24h
chainstream defi quote --chain solana --from SOL --to USDC --amount 1
```

デフォルトは JSON 出力。`--format=table` で人間向け。

## 次へ

<CardGroup cols={2}>
  <Card title="CLI リファレンス" icon="code" href="/jp/api-reference/cli-commands/overview">
    すべてのコマンド・フラグ・終了コード。
  </Card>
  <Card title="Agent Skills" icon="brain" href="/jp/docs/ai-agents/agent-skills/introduction">
    AI Agent が CLI を使うパターン。
  </Card>
</CardGroup>
