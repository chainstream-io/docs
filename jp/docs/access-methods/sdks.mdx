---
title: "SDK"
description: "TypeScript / Python / Go / Rust の型付きクライアントライブラリ — REST / GraphQL / WebSocket の慣用的ラッパー。"
icon: "box"
---

ChainStream SDK は、好きな言語からすべてのデータプロダクトを呼び出す人間工学的な方法を提供します。同じ [データプロダクト](/jp/docs/data-products/overview) と [アクセス方法](/jp/docs/access-methods/overview) に、型・リトライ・デフォルトが付いた形です。

## 言語

<CardGroup cols={2}>
  <Card title="TypeScript" icon="js" href="/jp/sdks/typescript">
    `@chainstream-io/sdk` — ブラウザ & Node 両対応。
  </Card>
  <Card title="Python" icon="python" href="/jp/sdks/python">
    `chainstream` — async ファースト。
  </Card>
  <Card title="Go" icon="golang" href="/jp/sdks/go">
    `github.com/chainstream-io/chainstream-go`。
  </Card>
  <Card title="Rust" icon="rust" href="/jp/sdks/rust">
    `chainstream` crate。
  </Card>
</CardGroup>

## インストール

```bash
npm install @chainstream-io/sdk
pip install chainstream
go get github.com/chainstream-io/chainstream-go
cargo add chainstream
```

## 最初の呼び出し — TypeScript

```ts
import { ChainStream } from "@chainstream-io/sdk";

const cs = new ChainStream({ apiKey: process.env.CHAINSTREAM_API_KEY! });

const results = await cs.tokens.search({
  chain: "solana",
  query: "USDC",
  limit: 5,
});

for (const t of results) {
  console.log(t.symbol, t.priceUsd);
}
```

## 次へ

<CardGroup cols={2}>
  <Card title="SDK カタログ" icon="box" href="/jp/sdks/overview">
    言語別ガイドとリリースノート。
  </Card>
  <Card title="Reference" icon="code" href="/jp/api-reference/overview">
    底層の REST / GraphQL / WebSocket。
  </Card>
</CardGroup>
