---
title: "CLI"
description: "`chainstream` 명령줄 인터페이스 — 셸에서 모든 데이터 제품과 DeFi 프리미티브에 접근."
icon: "terminal"
---

ChainStream CLI는 SDK / REST / GraphQL로 접근하는 [데이터 제품](/ko/docs/data-products/overview)의 CLI 입구입니다. `jq`, 셸 파이프라인, 셸을 구동하는 AI Agent와 조합하도록 설계되었습니다.

## 설치

```bash
brew install chainstream-io/tap/chainstream
npm install -g @chainstream-io/cli
curl -fsSL https://chainstream.io/install.sh | sh
```

## 인증

```bash
chainstream auth login
# 또는
export CHAINSTREAM_API_KEY=...
```

## 첫 명령어

```bash
chainstream tokens search --chain solana --query USDC --limit 5
chainstream trades tail --chain ethereum --token 0xA0b8...
chainstream rankings hot --chain solana --window 24h
chainstream defi quote --chain solana --from SOL --to USDC --amount 1
```

기본 출력은 JSON. 사람용은 `--format=table`.

## 다음 단계

<CardGroup cols={2}>
  <Card title="CLI 레퍼런스" icon="code" href="/ko/api-reference/cli-commands/overview">
    모든 명령어, 플래그, 종료 코드.
  </Card>
  <Card title="Agent Skills" icon="brain" href="/ko/docs/ai-agents/agent-skills/introduction">
    AI Agent가 CLI를 쓰는 패턴.
  </Card>
</CardGroup>
