---
title: "토큰"
description: "지원되는 모든 체인의 토큰 메타데이터, 시장 데이터, 가격, 통계, 생성 이벤트, 보안 시그널."
icon: "coins"
---

**Tokens** 데이터 제품은 ChainStream이 인덱싱하는 모든 대체 가능 토큰의 정규 소스입니다. 하나의 스키마, 체인별 주소 공간, 하나의 필드 셋 — 블루칩 ERC-20, 신규 Solana 밈코인, BSC 페어 모두 동일하게 다룰 수 있습니다.

## 필드

| 분류 | 예시 |
|:--|:--|
| Identity | `chainId`, `address`, `name`, `symbol`, `decimals`, `logo` |
| Market | `priceUsd`, `priceChange24h`, `marketCap`, `fdv`, `volume24h` |
| Supply | `totalSupply`, `circulatingSupply`, `holderCount` |
| Creation | `createdAtBlock`, `creatorAddress`, `firstTxHash`, `launchpad` |
| Security | `isHoneypot`, `canBuy`, `canSell`, `taxBuy`, `taxSell`, `owner` |
| Social | `website`, `twitter`, `telegram`, `verifiedSources` |

## 커버리지 & 신선도

- 체인: [지원 체인](/ko/docs/supported-chains) 전체.
- 신선도: 메타데이터는 생성 후 수 분, 시장 데이터는 초 단위.

## 접근

<CardGroup cols={2}>
  <Card title="REST" icon="database" href="/ko/api-reference/endpoint/data/token/v2/token-search-get">
    `/v2/token/search`, `/v2/token/{chain}/{tokenAddress}`, `/v2/token/{chain}/metadata/multi`, `/v2/token/{chain}/{tokenAddress}/security`.
  </Card>
  <Card title="GraphQL" icon="diagram-project" href="/ko/graphql/getting-started/overview">
    Tokens와 Trades / Holders / Candles JOIN.
  </Card>
  <Card title="WebSocket" icon="bolt" href="/ko/api-reference/endpoint/websocket/api">
    `dex-token-stats:{chain}_{tokenAddress}` 실시간 가격/거래량, `dex-new-token:{chain}` 신규 상장. SDK: `client.stream.subscribeTokenStats` / `subscribeNewToken`.
  </Card>
  <Card title="Kafka" icon="tower-broadcast" href="/ko/api-reference/kafka-topics/evm">
    `*.token.created`, `*.token.metadata`.
  </Card>
</CardGroup>

## 다음 단계

<CardGroup cols={2}>
  <Card title="Trades" icon="right-left" href="/ko/docs/data-products/trades">
    이 토큰의 모든 DEX 스왑.
  </Card>
  <Card title="Candles (OHLC)" icon="chart-column" href="/ko/docs/data-products/candles-ohlc">
    차트용 다구간 OHLC.
  </Card>
</CardGroup>
