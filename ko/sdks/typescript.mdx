---
title: 'TypeScript'
description: 'ChainStream TypeScript/JavaScript SDK'
icon: 'js'
---

## 설치

<CodeGroup>
```bash npm
npm install @chainstream-io/sdk
```

```bash yarn
yarn add @chainstream-io/sdk
```

```bash pnpm
pnpm add @chainstream-io/sdk
```
</CodeGroup>

## 빠른 시작

```typescript
import { ChainStreamClient } from '@chainstream-io/sdk';

const client = new ChainStreamClient('YOUR_ACCESS_TOKEN');
```

## REST API 예시

토큰 정보 조회:

```typescript
import { ChainStreamClient } from '@chainstream-io/sdk';

const ACCESS_TOKEN = 'YOUR_ACCESS_TOKEN';

async function main() {
  // SDK 클라이언트 초기화
  const client = new ChainStreamClient(ACCESS_TOKEN);

  const chain = 'sol';
  const tokenAddress = 'So11111111111111111111111111111111111111112'; // SOL

  console.log(`토큰 조회: ${chain}/${tokenAddress}`);

  try {
    // SDK를 통해 API 호출
    const result = await client.token.getToken(chain, tokenAddress);
    
    console.log('결과:');
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('API 호출 실패:', error);
  }
}

main();
```

## WebSocket 예시

실시간 토큰 캔들 데이터 구독:

```typescript
import { ChainStreamClient, Resolution } from '@chainstream-io/sdk';

const ACCESS_TOKEN = 'YOUR_ACCESS_TOKEN';

async function main() {
  const client = new ChainStreamClient(ACCESS_TOKEN, {
    autoConnectWebSocket: true,
  });

  const chain = 'sol';
  const tokenAddress = 'So11111111111111111111111111111111111111112'; // SOL

  console.log(`토큰 캔들 구독: ${chain}/${tokenAddress}`);
  console.log('리스닝 중... (Ctrl+C로 중지)\n');

  // SDK를 통해 WebSocket 구독
  client.stream.subscribeTokenCandles({
    chain,
    tokenAddress,
    resolution: Resolution['1s'],
    callback: data => {
      console.log(new Date().toISOString(), JSON.stringify(data));
    },
  });

  // 프로세스 유지
  process.on('SIGINT', () => {
    console.log('\n연결 종료 중...');
    process.exit(0);
  });

  // 실행 계속
  await new Promise(() => {});
}

main().catch(error => {
  console.error('테스트 실패:', error.message);
  process.exit(1);
});
```

## 추가 예시

### 토큰 검색

```typescript
const results = await client.token.searchToken('bonk');
console.log(results);
```

전체 파라미터는 [토큰 - 검색](/ko/api-reference/endpoint/data/token/v2/token-search-get)을 참조하세요.

### 거래 목록 조회

```typescript
const trades = await client.trade.getTrades('sol', {
  tokenAddress: 'TOKEN_ADDRESS',
  limit: 20,
});
console.log(trades);
```

전체 파라미터는 [거래 - 목록](/ko/api-reference/endpoint/data/trade/v2/trade-chain-get)을 참조하세요.

### 지갑 PnL 조회

```typescript
const pnl = await client.wallet.getPnl('sol', 'WALLET_ADDRESS');
console.log(`총 PnL: ${pnl.totalPnlUsd}`);
```

전체 파라미터는 [지갑 - PnL](/ko/api-reference/endpoint/data/wallet/v2/wallet-chain-walletaddress-pnl-get)을 참조하세요.

### 토큰 캔들(K선) 조회

```typescript
const candles = await client.token.getCandles('sol', 'TOKEN_ADDRESS', {
  resolution: '1h',
  limit: 24,
});
for (const c of candles) {
  console.log(`${c.time}: O=${c.open} H=${c.high} L=${c.low} C=${c.close}`);
}
```

전체 파라미터는 [토큰 - 캔들](/ko/api-reference/endpoint/data/token/v2/token-chain-tokenaddress-candles-get)을 참조하세요.

### 신규 토큰 조회 (랭킹)

```typescript
const newTokens = await client.ranking.getNewTokens('sol');
for (const token of newTokens) {
  console.log(`${token.symbol}: ${token.address}`);
}
```

전체 파라미터는 [랭킹 - 신규 토큰](/ko/api-reference/endpoint/data/ranking/v2/ranking-chain-newtokens-get)을 참조하세요.

---

## 에러 핸들링

```typescript
import { ChainStreamClient, ChainStreamError } from '@chainstream-io/sdk';

try {
  const token = await client.token.getToken('sol', 'INVALID_ADDRESS');
} catch (error) {
  if (error instanceof ChainStreamError) {
    console.error(`에러 코드: ${error.code}`);
    console.error(`에러 메시지: ${error.message}`);
  }
}
```

### 지수 백오프 재시도

속도 제한된 요청(HTTP 429)에 대한 재시도 로직 구현:

```typescript
async function withRetry<T>(fn: () => Promise<T>, maxRetries = 3): Promise<T> {
  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      return await fn();
    } catch (error) {
      if (error instanceof ChainStreamError && error.code === 429 && attempt < maxRetries) {
        const delay = Math.pow(2, attempt) * 1000;
        await new Promise(resolve => setTimeout(resolve, delay));
        continue;
      }
      throw error;
    }
  }
  throw new Error('Max retries exceeded');
}

const token = await withRetry(() => client.token.getToken('sol', 'TOKEN_ADDRESS'));
```

---

## 리소스

<CardGroup cols={2}>
  <Card title="GitHub" icon="github" href="https://github.com/chainstream-io/chainstream-sdk/tree/main/typescript">
    소스 코드 보기
  </Card>
  <Card title="npm" icon="npm" href="https://www.npmjs.com/package/@chainstream-io/sdk">
    패키지 레지스트리
  </Card>
</CardGroup>
